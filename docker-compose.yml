version: '3.8'

services:
  app:
    build: .
    container_name: loan_app
    ports:
      - "8000:8000"
    volumes:
      - .:/var/www
    depends_on:
      - db

  db:
    image: mysql:8
    container_name: loan_db
    restart: always
    environment:
      MYSQL_DATABASE: loan
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"